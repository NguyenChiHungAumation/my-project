using System;
using System.Collections.Generic;
using System.Linq;

class Program
{
    static void Main()
    {
        if (!int.TryParse(Console.ReadLine(), out int N) || N < 1 || N > 200) return;

        List<(string fromState, string even, string toState, string action)> state = new List<(string, string, string, string)>();

        for (int i = 0; i < N; i++)
        {

        }    
    } 
}
